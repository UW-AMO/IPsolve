Metadata-Version: 2.4
Name: ipsolve
Version: 0.1.0
Summary: Interior-point solver for piecewise linear-quadratic (PLQ) optimization
Author-email: "Aleksandr Y. Aravkin" <sasha.aravkin@gmail.com>
License-Expression: EPL-1.0
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: numpy>=1.24
Requires-Dist: scipy>=1.10
Provides-Extra: test
Requires-Dist: pytest>=7; extra == "test"
Requires-Dist: cvxpy>=1.4; extra == "test"
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Requires-Dist: cvxpy>=1.4; extra == "dev"
Requires-Dist: matplotlib>=3.6; extra == "dev"

# IPsolve – Python

A Python implementation of the **interior-point solver for piecewise
linear-quadratic (PLQ) composite problems**, based on the JMLR paper:

> Aravkin, Burke, Pillonetto.
> *Sparse/Robust Estimation and Kalman Smoothing with Nonsmooth
> Log-Concave Densities: Modeling, Computation, and Theory.*
> JMLR 14(82):2689–2728, 2013.

## Quick start

```bash
pip install -e ".[test]"           # editable install with test deps
pytest                             # run all tests (uses CVXPY to verify)
```

## What it solves

$$
\min_{x} \;\; \ell^{\!\top} x \;+\; \rho_{\text{meas}}\!\bigl(z - Hx\bigr)
  \;+\; \rho_{\text{proc}}\!\bigl(Kx - k\bigr)
  \quad\text{s.t.}\quad Ax \le a
$$

where $\rho_{\text{meas}}$ and $\rho_{\text{proc}}$ are **PLQ penalties**
(ℓ₁, ℓ₂, Huber, hinge, quantile, Vapnik, …) represented in their
conjugate/dual form via matrices $(M, B, C, c, b)$.

## Design

| Module | Purpose |
|--------|---------|
| `ipsolve.plq` | PLQ dataclass and `stack` / `compose` helpers |
| `ipsolve.penalties` | Penalty library (`l1`, `l2`, `huber`, `hinge`, …) |
| `ipsolve.kkt` | KKT residual and Newton solve (barrier formulation) |
| `ipsolve.solver` | Interior-point loop with Mehrotra corrector |
| `ipsolve.api` | High-level `solve(H, z, meas, proc, …)` entry point |
| `ipsolve.nonlinear` | Gauss-Newton wrapper for convex-composite problems |

## Examples

See `examples/` for runnable scripts covering:
- Lasso / elastic net
- Robust regression (Huber)
- SVM (hinge loss)
- Kalman smoothing (standard + robust)
- CVaR portfolio optimisation
- Quantile regression
- Nonlinear convex-composite
